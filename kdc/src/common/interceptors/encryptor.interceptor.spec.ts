import { EncryptorInterceptor } from './encryptor.interceptor';

describe('EncryptorInterceptor', () => {
  it('should be defined', () => {
    expect(new EncryptorInterceptor()).toBeDefined();
  });
});
