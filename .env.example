# Consumed by docker-compose. Copy to .env and fill in.
#   openssl rand -hex 24
REDIS_PASSWORD=
POSTGRES_DB=kerberos
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# Host ports for docker-compose. Change these if 6379/5432/8080 are taken.
REDIS_PORT=6379
POSTGRES_PORT=5432
ADMINER_PORT=8080
